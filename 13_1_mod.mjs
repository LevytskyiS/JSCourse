const sum = (a, b) => a + b
const one = 1
const two = "2"

// export default sum - это для экспорта 1 переменной
// Для нескольких переменных, используем след. синтаксис
// Все export инструкции должны быть внизу файла

export {
    sum, 
    one,
    two
}