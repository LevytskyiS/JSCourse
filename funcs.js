let a = 6
let b = 3

function sum(a, b) {
    const c = a + b
    console.log(c)
    return c
}

// console.log(sum(a, b))
sum(a, b)

// Функция может быть: 
// Именованной
// Присвоена переменной
// Анонимной
// Аргументом при вызове другой функции
// Значением свойства (метода) объекта